
1. Create iOS Framework Project

2. pod spec create RM.iOS.UIKit

3. git push -u origin master --tags

// push to pod repo

4. pod repo add RM.iOS.UIKit https://github.com/ruchiralk/RM.iOS.UIKit.git

5. pod repo push RM.iOS.UIKit.podspec
